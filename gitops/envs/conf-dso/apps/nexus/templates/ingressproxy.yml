---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: docker-proxy
  namespace: dso-nexus
  annotations:
    cert-manager.io/cluster-issuer: acme-issuer
    route.openshift.io/termination: edge
  labels:
    app: nexus
spec:
  tls:
  - hosts:
    - <path:dso-gcp-dev/data/env/conf-dso/apps/global/values#domain | jsonPath {.nexusDockerProxy}>
    secretName: nexus-docker-proxy-tls-secret
  ingressClassName: <path:dso-gcp-dev/data/env/conf-dso/apps/global/values#ingressClassName>
  rules:
    - host: <path:dso-gcp-dev/data/env/conf-dso/apps/global/values#domain | jsonPath {.nexusDockerProxy}>
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nexus
                port:
                  name: dockerproxy
