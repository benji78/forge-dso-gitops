---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vault-ingress
  namespace: dso-vault
  annotations:
    cert-manager.io/cluster-issuer: acme-issuer
    route.openshift.io/termination: edge
  labels:
    app: vault
spec:
  tls:
    - hosts:
        - <path:dso-s3ns-dev/data/env/conf-dso/apps/global/values#domain | jsonPath {.vault}>
      secretName: vault-tls-secret
  ingressClassName: <path:dso-s3ns-dev/data/env/conf-dso/apps/global/values#ingressClassName>
  rules:
    - host: <path:dso-s3ns-dev/data/env/conf-dso/apps/global/values#domain | jsonPath {.vault}>
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: conf-dso-vault-active
                port:
                  number: 8200
